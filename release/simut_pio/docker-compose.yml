services:

  # Compiles firmware for the Raspberry Pi Pico W
  build:
    build: .
    user: "${UID:-1000}:${GID:-1000}"
    volumes:
      - ".:/workspace"
    command: pio run -e pico_w_release

  # Runs both native unit test suites
  test:
    build: .
    user: "${UID:-1000}:${GID:-1000}"
    volumes:
      - ".:/workspace"
    command: >
      sh -c "pio test -e native && pio test -e native_history_v5"
